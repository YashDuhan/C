#include <stdio.h>
#include <conio.h>
int main()
{
        int a, b;
        printf("Enter a number : ");
        scanf("%d", &a);
        b = a * a - 1 * a - 2 * a - 3 * a - 4;
        printf("Output is =%d", b);
        getch();
}
